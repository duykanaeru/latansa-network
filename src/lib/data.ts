import { Customer, InternetPackage, Payment, Ticket, Technician, TicketActivity } from './types';

// Data kosong - semua data akan diambil dari API MongoDB
// File ini tetap ada untuk backward compatibility

export const mockPackages: InternetPackage[] = [];

export const mockCustomers: Customer[] = [];

export const mockPayments: Payment[] = [];

export const mockTechnicians: Technician[] = [];

export const mockTickets: Ticket[] = [];

export const mockTicketActivities: TicketActivity[] = [];
